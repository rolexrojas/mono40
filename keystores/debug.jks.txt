# Command
keytool -genkey -v -keystore debug.jks -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000
# Date
2016-09-13


, (dialog, which) -> {
                    if (message.contains(getString(R.string.session_expired))) {
                        this.startActivity(InitActivityBase.getLaunchIntent(getContext()));
                        getActivity().finish();
                    }
                }