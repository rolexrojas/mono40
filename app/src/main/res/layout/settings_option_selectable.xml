<?xml version="1.0" encoding="utf-8"?>
<merge
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <ImageView
    android:id="@+id/indicator"
    android:layout_width="@dimen/tinyIconSize"
    android:layout_height="@dimen/tinyIconSize"
    android:layout_alignParentEnd="true"
    android:layout_centerVertical="true"
    android:layout_marginStart="@dimen/normalHorizontalSpace"
    android:visibility="gone"
    app:srcCompat="@drawable/settings_option_selectable_indicator"/>

  <ImageView
    android:id="@+id/icon"
    android:layout_width="@dimen/normalIconSize"
    android:layout_height="@dimen/normalIconSize"
    android:layout_centerVertical="true"
    android:layout_marginEnd="@dimen/smallHorizontalSpace"
    android:visibility="gone"/>

  <TextView
    android:id="@+id/primaryTextView"
    style="@style/SettingsText.Primary"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_centerVertical="true"
    android:layout_toEndOf="@+id/icon"
    android:layout_toStartOf="@+id/indicator"
    android:ellipsize="end"/>
</merge>